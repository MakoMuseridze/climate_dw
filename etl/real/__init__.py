"""Real-source extractors. Each module defines an Extractor subclass that
downloads from the source's real URL, parses the real format, harmonizes
country codes to country_id, and returns rows matching its target fact table."""
